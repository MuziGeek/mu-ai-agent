# 2026-09-15

## 分析 pgvector 构建初始化链路（只读分析，未改代码）

- 技术栈：Spring Boot 3.4.5 + Spring AI 1.1.8 + Alibaba DashScope（text-embedding-v3，1024 维）
- pgvector 走的是**纯自动配置**：`spring-ai-starter-vector-store-pgvector` →
  `PgVectorStoreAutoConfiguration`（after JdbcTemplateAutoConfiguration），
  条件 = `@ConditionalOnClass(PgVectorStore, DataSource, JdbcTemplate)` +
  `@ConditionalOnProperty(name=spring.ai.vectorstore.type, havingValue=pgvector, matchIfMissing=true)`
- DDL 在 `PgVectorStore.afterPropertiesSet()` 里执行，顺序：
  CREATE EXTENSION vector → hstore → uuid-ossp（仅 id-type=uuid 时）→ CREATE SCHEMA →
  （可选 DROP TABLE）→ CREATE TABLE → CREATE INDEX，**全部带 IF NOT EXISTS**
- 关键默认值（1.1.8，从字节码确认）：table=`vector_store`、schema=`public`、
  index=`spring_ai_vector_index`、id-type=uuid、index-type=hnsw、distance=cosine、
  `initializeSchema=false`、**`schemaValidation=false`**、`dimensions=-1`、
  fallback 维度 1536
- 建表模板：`CREATE TABLE IF NOT EXISTS %s (id %s PRIMARY KEY, content text, metadata json, embedding vector(%d))`

### 发现的问题（未修复）
1. `application-local.yml` 注释里的 `spring.ai.vectorstore.pgvector.enabled: false` 在 1.1.8 **不存在**此属性，禁用无效；正确写法是 `spring.ai.vectorstore.type: none`
2. `simple-vector` profile 下 PgVector 仍会创建（`@ConditionalOnMissingBean` 按返回类型 PgVectorStore 判定），且 RagApp/DocumentLoader 的参数名 `vectorStore` 与自动配置 bean 同名 → 实际注入仍是 PgVector，profile 切换形同虚设
3. `src/main/resources/document/` 目录不存在 → DocumentLoader 永远扫到 0 个文档
4. 无 ApplicationRunner/CommandLineRunner/Controller 调用 `RagApp.initKnowledgeBase()`，知识库不会自动初始化
5. 表已存在时 `IF NOT EXISTS` 不会更新列定义，改维度（如 1536→1024）不生效，需手动 DROP/ALTER

### 环境备注
- Bash 工具本会话 `dirname`/`head` 报 command not found（PortableGit shim 异常），改用 PowerShell + 写临时文件再 Read
- 反编译用 `D:\Scoop\apps\temurin21-jdk\current\bin\javap.exe`；jar 用 Expand-Archive 解压后加 classpath

## 新增 PgVector 集成测试类

- 文件：`src/test/java/com/muzi/muaiagent/rag/PgVectorVectorStoreConfigTest.java`（新建）
- 4 个用例：写入+相似度检索 / 相似度阈值过滤 / 元数据过滤检索 / 按元数据删除
- 注入方式 `@Resource(name = "vectorStore")`，显式按 Bean 名注入，规避 simple-vector profile 下的双 Bean 歧义
- 所有用例加 `@Transactional`，写入自动回滚，不污染向量库，可反复执行
- 断言写成「不过度约束」风格（如 `allMatch` 元数据条件、`size() <= topK`），
  避免因表里已有历史数据而误报 —— 实测表里确实已有 2 条同内容历史行
- 实测：`mvn test -Dtest=PgVectorVectorStoreConfigTest` → Tests run: 4, Failures: 0，BUILD SUCCESS
- 实测相似度（text-embedding-v3 / 查询 "Spring"）：命中 Spring 的文档 0.6384，无关文档 0.5113
  → 注意：DashScope 的余弦相似度整体偏高（无关文本也 >0.5），阈值不要设得太乐观
- 启动日志确认：`Initializing PGVectorStore schema for table: vector_store in schema: public`、
  `vectorTableValidationsEnabled false`

### 待清理（需用户确认）
- `vector_store` 表内已有 2 条历史遗留行（meta1=meta1，内容与测试样例相同），
  推测是先前以非事务方式跑过官方示例留下的，未擅自删除

## 目录路径对齐 + 补知识库初始化入口

- 用户此前添加了 `document/`，但目录结构是 `document/DDD/`，与代码里的
  `classpath:document/Java8Gu5/DDD/**/*.md` 对不上 → 仍扫不到
- 用户选择「移动目录」而非改代码：已把 `src/main/resources/document/DDD/`
  整体移到 `src/main/resources/document/Java8Gu5/DDD/`（13 个文件，其中 7 个 .md）
  - 移动前该目录是 git untracked（`?? src/main/resources/document/`），未用 git mv
  - 注意目录里有一层多余的嵌套 `DDD/DDD/`（疑似解压产生），`**` 通配符能覆盖，未动它
- 用户选择「在测试代码类里添加」触发入口（不加 ApplicationRunner / Controller）：
  `PgVectorVectorStoreConfigTest` 新增 `@Resource RagApp ragApp` + `testInitKnowledgeBase()`
- 实测 `mvn -B test -Dtest=PgVectorVectorStoreConfigTest` → Tests run: **5**, Failures: 0，BUILD SUCCESS
  - 扫描 7 个文件 → 12 个分块 → 2 批写入（10 + 2）
  - 查询「什么是聚合根？」→ 相似度 **0.8209** 命中 `✅什么是聚合，什么是聚合根？.md`（source 元数据正确）
  - 第二名 0.4438 来自 `✅请详细描述DDD的实现流程？.md`，区分度良好
- **回滚已验证有效**：两次运行中 meta1 命中文档数都是 3（未随运行次数增长），
  证明 `@Transactional` 确实把灌库写入回滚掉了，不会重复堆积
- 遗留小问题：控制台日志里 `✅` 显示为 `?`（终端 GBK 编码），实际文件名正常，非代码问题
- 遗留小问题：`DocumentLoader` 的 `@Value("classpath:document/Java8Gu5/DDD") Resource dddDir`
  字段从未被使用（纯死代码），未清理

## 排查 vector_store 表实际状态（重要，纠正先前判断）

用临时 JDBC 程序（`%TEMP%/DumpVectorStore.java`，只读）导出全表，**实际 18 行，不是 2 行**：

| 条数 | 内容 | 元数据 |
|---|---|---|
| 12 | DDD 知识库分块 | 带 `source` / `chunk_index` / `total_chunks` / `parent_document_id` |
| 6 | 「Spring AI rocks」/「The World is Big」/「You walk forward」**各 2 份副本** | 分别为 `meta1` / `{}` / `meta2` |

- **纠正**：先前我说「2 条遗留行」是基于 `testMetadataFilter` 里 meta1 命中 3 条反推的，
  只覆盖了带 meta1 的那一类。实际遗留的是**整份 3 条样例的 2 份副本 = 6 行**
- 表结构确认：`id uuid` / `content text` / `metadata json` / `embedding vector`；
  扩展 `vector` `hstore` `uuid-ossp` `plpgsql`；
  索引 `vector_store_pkey`(btree id) + `spring_ai_vector_index`(hnsw, vector_cosine_ops)
- `metadata` 是 **json 不是 jsonb**，所以 `metadata ? 'source'` 这种 jsonb 操作符会报错，
  要用 `metadata->>'source'`

### 回滚机制验证（本次做了决定性实验）
- 只跑 `-Dtest=PgVectorVectorStoreConfigTest#testInitKnowledgeBase` 前后各 dump 一次
- 结果：**18 行 → 18 行，无增长** → `@Transactional` 回滚确实生效
- 佐证：`target/surefire-reports` 只有 3 次记录（10:24 / 11:23 / 14:23），源码最后修改时间
  为 11:23:15，期间无人改动代码
- 推测那 12 条 DDD 分块来自**非 Maven 的执行**（如 IDE 里临时去掉 `@Transactional` 跑了一次，
  IDE 运行不会写入 surefire-reports），机制未最终确认

## 项目其他发现
- `.gitignore` 的 CUSTOM 段包含：`.env`、`/src/main/resources/application-local.yml`、
  **`/src/test/`（整个测试目录被忽略，所以测试类不会进版本库）**、
  `/src/main/resources/document/Java8Gu5/`（知识库文档有意不入库）
- ⚠️ `git status` 显示**仓库根目录有一个 `application-local.yml` 处于 `AD` 状态**
  （已 git add 进索引、工作区已删除）—— 索引里那份含明文 RDS 密码与 DashScope Key，
  一旦提交就会把密钥写进版本库，需处理

## 执行清库（用户确认后）

- 用户选择：**清空整表 18 行** + **把含密钥的 application-local.yml 从暂存区移除**
- 清库前先做完整备份：`D:\GitProject\mu-ai-agent\.workbuddy\backup\vector_store_backup_2026-09-15.sql`
  （248 KB，18 条 INSERT，含 id/content/metadata/embedding，可直接 psql 恢复）
- 清库结果：**18 行 → 0 行**；表结构、4 个扩展、`vector_store_pkey` 与
  `spring_ai_vector_index`(hnsw) 均完好无损
- 安全约束：脚本仅在「实际行数 == 预期 18」且「导出条数 == 18」时才执行 DELETE，否则中止
- git：已执行 `git rm --cached application-local.yml`，暂存区已干净
  - 注意 `.gitignore` 只忽略了 `/src/main/resources/application-local.yml`（带路径锚点），
    **没有覆盖根目录同名文件**，仍有再次被误 add 的风险

## 可复用的小工具（临时文件，位于 %TEMP%）
- `%TEMP%/DumpVectorStore.java`：只读导出 vector_store 全表 + 表结构 + 扩展/索引
- `%TEMP%/ClearVectorStore.java`：先备份再清空（带行数校验）
- 运行方式（**不要**把数据库配置写进项目）：
  `java "-cp" <postgresql-42.7.5.jar> %TEMP%/DumpVectorStore.java`
  - PostgreSQL 驱动在 `D:\Scoop\apps\maven\3.9.16\rep\org\postgresql\postgresql\42.7.5\`
  - 用 Java 21 的单文件源码启动器，无需把临时代码加进项目
- **PowerShell 坑**：`-Dfile.encoding=UTF-8` 这类带点的参数会被 PowerShell 拆坏
  （报「找不到主类 .encoding=UTF-8」），必须加引号 `"-Dfile.encoding=UTF-8"`
- `metadata` 是 `json` 不是 `jsonb`，`metadata ? 'key'` 会报 operator does not exist，
  要用 `metadata->>'key'`

## 清理死代码 + 补 RAG 问答入口

### 1. `DocumentLoader.dddDir` 死代码已删
- 删掉从未使用的 `@Value("classpath:document/Java8Gu5/DDD") Resource dddDir` 字段
  及对应的 `org.springframework.beans.factory.annotation.Value` import
- 顺手把重复的魔法字符串提取为常量
  `private static final String DDD_DOCUMENT_PATTERN = "classpath:document/Java8Gu5/DDD/**/*.md"`
- 注意：`org.springframework.core.io.Resource` 的 import **仍需保留**（`Resource[]` 在用）

### 2. `RagApp.doChatWithRag()` 入口已加到测试类
- `PgVectorVectorStoreConfigTest` 新增 `testChatWithRag()`，打了 **`@Tag("llm")`**
  → 因为它真实调用 DashScope qwen-plus，有网络延迟与 token 成本，且回答不确定
  - 排除它跑快速回归：`mvn -B test -Dtest=<类> -DexcludedGroups=llm`（实测 5 个用例全绿）
  - 单独跑它：`mvn -B test -Dtest=PgVectorVectorStoreConfigTest#testChatWithRag`
- 用例结构：先 `initKnowledgeBase()` → **确定性断言**检索命中 → 再调 `doChatWithRag()` 断言回答非空白
- `@Transactional` 下「先写后读」可见性再次得到验证：灌库后同一事务内检索正常命中
- 实测（31.6s，含大模型往返）→ Tests run: 1, Failures: 0，BUILD SUCCESS
  - 检索：0.8209 `✅什么是聚合，什么是聚合根？.md` / 0.4438 `✅请详细描述DDD的实现流程？.md`
    / 0.4339 `✅什么是充血模型和贫血模型？.md`
  - 回答确实**基于知识库**（提到了「订单聚合 / Order / OrderItem」「充血模型」，
    正是命中文档里的内容），说明 QuestionAnswerAdvisor 拼接上下文生效
- `doChatWithRag` 的实现细节：`chatResponse` 为 null 时返回 null 而非抛异常，故断言用 `assertNotNull`

## 实现批处理策略（BatchingStrategy）

### 结论：项目原本**没有**自定义批处理策略
- 全项目 grep `BatchingStrategy|batching|TokenCountBatching` → **零命中**
- 实际生效的是 `PgVectorStoreAutoConfiguration` 自带的默认策略
- `DocumentLoader` 里手动 `batchSize=10` 切片，就是绕开 DashScope 单次条数限制的临时手段

### 字节码核实的关键事实（Spring AI 1.1.8 + alibaba dashscope 1.1.2.1）
- `PgVectorStoreAutoConfiguration` 有 `@Bean @ConditionalOnMissingBean BatchingStrategy
  pgVectorStoreBatchingStrategy()` → `new TokenCountBatchingStrategy()`（无参）
- `PgVectorStore.doAdd()`：`embeddingModel.embed(docs, options, batchingStrategy)`；
  落库另按 `maxDocumentBatchSize`(10000) 切片 —— **两个上限是分开的**
- `DashScopeEmbeddingModel` 覆写了 3 参 `embed(...)`，但内部只是补默认 options 后
  `invokespecial AbstractEmbeddingModel.embed(...)` → 走 `EmbeddingModel` 的 default 方法
  → `for (batch : batchingStrategy.batch(docs)) { call(new EmbeddingRequest(texts, options)); }`
  → **每个批次 = 一次 DashScope 请求，策略真实生效**
- `EmbeddingModel` default 实现的 3 个要点：
  1. `Assert.notNull(documents, "Documents must not be null")`
  2. 逐批 `call()`，异常会中断整次 add
  3. **按位置回填向量** → 策略必须保序
- `TokenCountBatchingStrategy`：`MAX_INPUT_TOKEN_COUNT=8191`、`DEFAULT_TOKEN_COUNT_RESERVE_PERCENTAGE=0.1`；
  三参构造 → `(encodingType, max, reserve, Document.DEFAULT_CONTENT_FORMATTER, MetadataMode.NONE)`；
  实际预算 `Math.round((1-reserve) * max)`；**只按 token 切，条数无上限**；
  单文档超限抛 `IllegalArgumentException("Tokens in a single document exceeds the maximum number of allowed input tokens")`
- `jtokkit:1.1.0` 经 `spring-ai-tika-document-reader → spring-ai-commons` 传递引入（compile），
  未在 pom 里显式声明，直接 import `EncodingType` 可编译

### 改动
1. 新增 `rag/config/DashScopeBatchingStrategy.java`
   - 两级切分：先按 `maxDocumentsPerRequest` 分组，再委托 `TokenCountBatchingStrategy` 按 token 细分
   - 不复制子列表，用 `subList` 视图切片；保序拼接
   - 捕获委托器的 `IllegalArgumentException`，补上「哪个 `source` 文件超限 + 预估 token」再抛
     - 明细**刻意拼成单行**：异常信息常被塞进一行日志，带 `\n` 会打断日志格式（第一版踩过）
   - 日志：DEBUG 打印每组条数/预估 tokens/子批数；INFO 打印 `文档数 → 批次总数`
   - 自带 `JTokkitTokenCountEstimator` 仅用于日志与异常定位（口径与委托器一致：NONE + DEFAULT_CONTENT_FORMATTER）
2. 新增 `rag/config/EmbeddingBatchingConfig.java`：注册 `@Bean BatchingStrategy batchingStrategy(...)`，
   三个参数带默认值 `10 / 8000 / 0.1`，可用 `mu-ai.rag.batching.*` 覆盖
3. `DocumentLoader`：删掉手动 10 条切片循环，改 `vectorStore.add(allChunks)` 一次性提交
   - 单一事实来源：厂商限制只存在于策略一处
4. 新增 `src/test/java/com/muzi/muaiagent/rag/EmbeddingBatchingStrategyTest.java`（5 个用例）
   - 断言注入的 `BatchingStrategy` 是 `DashScopeBatchingStrategy`（覆盖机制静默失效的防线）
   - 12 文档 → `[10, 2]`；25 文档 → 保序且总数不变；超长文档抛异常且信息含 source；空/null 返回空批次

### 实测
- `EmbeddingBatchingStrategyTest`：Tests run: 5, Failures: 0
- 全量（`-DexcludedGroups=llm`）：**Tests run: 11, Failures: 0，BUILD SUCCESS**
- 端到端：`批处理完成：12 个文档 → 2 个批次（单批上限 10 个文档）` →
  `共写入 12 个文档分块` → `知识库初始化完成，共导入 7 个文档`
- 检索得分与改造前**完全一致**（0.8209 / 0.4438 / 0.4339）→ 改造是无损的

## 把 jtokkit 提为显式依赖

> 纠正上文「实现批处理策略」节末尾的说法：`jtokkit` 当时**未显式声明**，现已补上。

- pom.xml 在 `spring-ai-tika-document-reader` 之后新增：
  ```xml
  <dependency>
      <groupId>com.knuddels</groupId>
      <artifactId>jtokkit</artifactId>
      <version>1.1.0</version>
  </dependency>
  ```
- **必须写 `<version>`**：先 grep 了 `spring-ai-bom-1.1.8.pom`，其中**不含** jtokkit 条目
  → BOM 不管理该依赖，省掉版本会报 `'dependencies.dependency.version' is missing`
- 版本锁定在 1.1.0，与原传递引入版本一致，**运行时行为零变化**
- 依据：`DashScopeBatchingStrategy` 直接 import `EncodingType`，属「直接使用的库应显式声明」；
  传递依赖一旦上游调整会被无声移除，只会在编译期才暴露
- 验证：`mvn -B -Dincludes=com.knuddels:jtokkit dependency:tree test-compile`
  → 输出 `\- com.knuddels:jtokkit:jar:1.1.0:compile`（单条，无重复）+ `BUILD SUCCESS`
- 回归：`mvn -B test -DexcludedGroups=llm` → **Tests run: 11, Failures: 0，BUILD SUCCESS**
  - 批处理日志与改动前逐字一致：`12 个文档 → 2 个批次`、`25 个文档 → 3 个批次`

## 修复 IDEA 报「找不到符号 变量 log」（Lombok 注解处理器失效）

### 现象
- IDEA 编译报 `InterViewApp.java:150:9 java: 找不到符号 变量 log`
- 但源码里 `@Slf4j`（第 43 行）和 `import lombok.extern.slf4j.Slf4j`（第 8 行）都在
- **关键线索**：报错格式 `java: 找不到符号` 是 **IDEA 编译器**的输出（Maven javac 会带
  `[ERROR] ...java:[150,9]` 这种格式）→ 说明不是代码问题

### 定位过程
- `mvn -B clean compile` → **BUILD SUCCESS**，确认代码与 pom 对 Maven 都正常
- 读 `.idea/compiler.xml` → 找到元凶：
  `<entry name="$PROJECT_DIR$/../../Scoop/apps/maven/3.9.16/rep/org/projectlombok/lombok/unknown/lombok-unknown.jar" />`
  → 版本被解析成 **`unknown`**，指向不存在的 jar（`Test-Path` 返回 False）
- 本地仓库实际只有 `1.18.38`；`lombok/unknown/` 下仅两个 `.lastUpdated` 空壳文件

### 根因
`pom.xml` 里 `maven-compiler-plugin` 的 `<annotationProcessorPaths><path>` **没写 `<version>`**：
- Maven：缺 version 会回溯 dependencyManagement（spring-boot-dependencies 管理 lombok）→ 正常
- IDEA：**不回溯父 POM**，版本解析为 `unknown` → 生成无效 processorPath →
  Lombok 处理器根本没加载 → 不生成 `log` 字段

### 修复（两处）
1. `pom.xml`（根治）：`<path>` 内加 `<version>${lombok.version}</version>`
   - 先 grep 确认 `spring-boot-dependencies-3.4.5.pom:125` 有 `<lombok.version>1.18.38</lombok.version>`
   - 附详细注释说明「为什么不能省」
2. `.idea/compiler.xml`（止血）：把路径里的 `unknown/lombok-unknown.jar`
   改为 `1.18.38/lombok-1.18.38.jar`
   - `workspace.xml` 里无注解处理相关配置（已 grep 确认），无需改

### 验证
- `mvn -B clean compile` → BUILD SUCCESS
- `mvn -B help:effective-pom` → **三处** `annotationProcessorPaths`（default-compile、
  default-testCompile、plugin 级 configuration）**全部**插值为 `<version>1.18.38</version>`
- `mvn -B test -DexcludedGroups=llm` → Tests run: 11, Failures: 0，BUILD SUCCESS
- IDEA 侧需 Reload Maven 项目，让它按新 pom 重写 `.idea/compiler.xml`

### 可复用判据（写进 MEMORY.md）
「Maven 编译过、IDEA 报找不到符号（log/其他 Lombok 产物）」→ 先查 `.idea/compiler.xml`
的 processorPath 是否含 `unknown`，再查 pom 的 `annotationProcessorPaths` 是否缺 version

### 顺带清理
- 已删除残留空壳目录 `D:\Scoop\apps\maven\3.9.16\rep\org\projectlombok\lombok\unknown\`
  （仅 2 个 `.lastUpdated`，共 982 字节，无 jar）
- 删前已备份到 `%TEMP%\lombok_unknown_backup\`
- 删后校验：`unknown` 不存在；`1.18.38\lombok-1.18.38.jar`(2,056,090 bytes) 完好；
  `mvn -B clean compile` → BUILD SUCCESS
- **坑**：首次 `Remove-Item -Recurse -Force -Confirm:$false` 返回 exit 1（疑似文件瞬时被占用），
  去掉 `-Confirm:$false` 重跑即成功。PowerShell 工具不回显 stderr，排查时务必用
  `try/catch` + 写文件的方式捕获 `$_.Exception.Message`

## 提交已推送尝试（本地提交成功，推送被环境阻断）

### 已完成：本地两个提交
```
a5c0de2 chore : 补充项目工作记录与向量库备份快照
347ec45 feature : 新增 DashScope 批处理策略（条数 + token 双上限）
260b5d5 ← origin/main（推送前基线）
```
- `git add -A` 后自检确认**未包含** `application-local.yml` / `src/test/` / `.idea`
  （均在 .gitignore 内），密钥无泄露风险
- 中文提交信息正常（PowerShell 需先 `[Console]::OutputEncoding = [Text.Encoding]::UTF8`，
  否则 commit message 会乱码；用 here-string `@'...'@` 传多行消息）

### 推送失败：git 的 schannel TLS 握手被代理阻断
逐层排查后的**决定性对比**（同一代理 127.0.0.1:7897）：

| 通道 | 结果 |
|---|---|
| `curl.exe` → github.com | ✅ ssl=0.57s / 1.62s / 0.59s |
| `curl.exe` → git 端点 `/info/refs?service=git-upload-pack` | ✅ code=200 ssl=0.58s |
| `curl.exe` → `/info/refs?service=git-receive-pack`（写权限） | ✅ code=200 ssl=0.62s |
| `git ls-remote origin` | ✅ 成功 |
| `git push origin main` | ❌ **schannel: failed to receive handshake**（5 秒超时） |

- `GIT_CURL_VERBOSE=1` 抓到的关键序列：
  `CONNECT 200 Connection established` → `ALPN: curl offers http/1.1` →
  **恰好 5 秒后** `schannel: failed to receive handshake, SSL/TLS connection failed`
  → CONNECT 隧道建好了，但 TLS 握手在 5s 内收不到回应
- 已穷尽尝试且**全部 128**：`http.sslBackend=openssl`（`-c` 与 `--global` 都试过）、
  `http.version=HTTP/1.1`、显式 `http.proxy`、`http.postBuffer`、
  `http.extraHeader=Connection: keep-alive`、`CURL_SSL_BACKEND=openssl`、
  直连绕过代理（3 次全失败）
- 注意 `D:\Git\mingw64\bin\` 下同时有 `libcurl-4.dll`(schannel 版) 与
  `libcurl-openssl-4.dll`，但 git 未按 `http.sslBackend` 切换实际加载的 curl 库
- SSH 通道**不可用**：沙箱明确拒绝读取 `C:\Users\HP\.ssh\*`（31 项），
  `ssh -T git@github.com` 报 `Host key verification failed`
- 诊断脚本（临时 Python CONNECT 中继）**未跑通**：`Start-Process` 被沙箱拒绝

### 认证部分：已解决
- 诊断初期 push 返回 **空输出 + exit 128**，真因是 **GCM 需要交互式授权**
  （`git-credential-manager.exe get` 报 `fatal: Cannot prompt because user
  interactivity has been disabled`，且环境里没有缓存凭据）
- 用户选择「重新授权交互式登录」后，执行
  `git-credential-manager.exe github login` → `GCM_LOGIN_EXIT=0`
  → 之后 `get` 能返回 `username=MuziGeek` + 40 位 `gho_` token
- 用该 token 走系统 curl 访问 `api.github.com/repos/MuziGeek/mu-ai-agent` → **code=200**
  → **凭据有效且有仓库权限**
- ⚠️ 本次 `github login` 产出的是**短期 OAuth token**（`gho_` 前缀），
  会随登录有效期过期；用于 CLI 推送建议改用 PAT，或让 GCM 走浏览器授权
- ⚠️ 过程中 token 明文落入过 `%TEMP%` 文件，已全部删除并扫描确认无残留

### 结论
**代码提交已完成，推送受阻于「git 自带 curl + schannel 经 7897 代理握手超时」这一本机环境问题**，
与仓库、凭据、代码均无关。可行出路（需用户选）：
1. 用户在**自己的交互式终端**执行 `git push origin main`（通常能成功，GUI 环境无沙箱限制）
2. 换 SSH remote（用户需自行配好 key，沙箱内无法读取 ~/.ssh）
3. 用 curl 手工走 git-receive-pack 协议（可行但需手写 pkt-line + pack 组装，较脆弱）
4. 排查 mihomo 的 TLS 处理（如关闭 `sniff` / 改 `tls-fingerprint` / 换节点）

---

## 【更正】push 失败真因查清：凭据助手错位，非 TLS（当日晚 18:00）

**上文「schannel 经 7897 握手超时」的结论是误判，作废。** 重新排查后定位到真正原因。

### 排查过程与证据
- `GIT_TRACE=1 GIT_CURL_VERBOSE=1 git push` 落盘日志显示：CONNECT 隧道 200 →
  `ALPN: server accepted http/1.1` → `schannel renegotiated` 成功 → 收到
  `HTTP/1.1 401 Unauthorized`（GitHub 对 receive-pack 未认证必然返 401，属正常）
  → 转为调用凭据助手 → `fatal: could not read Username` → 进程挂到 90s 超时。
  **整条 TLS 链路完全正常，日志中没有任何握手失败。**
- 当前 Bash 会话的 `git` 是 **PortableGit 2.55.0.windows.3**（`/mingw64/bin/git`），
  不是 `D:\Git` 的 2.55.0.windows.5，两者凭据助手**不同源**：
  - PortableGit 系统级配置：`credential.helper = helper-selector`
  - `D:\Git\etc\gitconfig`：`credential.helper = manager`
- 三组对照：
  | 调用 | 结果 |
  |---|---|
  | `git credential-helper-selector get` | ❌ 挂起，超时 124、无输出（等 GUI 对话框） |
  | PortableGit 自带 `git-credential-manager.exe get` | ✅ `username=MuziGeek` + token，exit 0 |
  | `git -c credential.helper=<绝对路径 GCM> push` | ✅ **推送成功** `260b5d5..a5c0de2 main -> main` |

### 修复（`~/.gitconfig`）
```
credential.helper = ""        # 空值清空此前累积的助手列表（含系统级 helper-selector）
credential.helper = manager   # 只留 GCM
```
关键点：`credential.helper` 是**多值追加**语义，只 add `manager` 顶不掉系统级的
`helper-selector`（它仍先挂起）；**必须先写空值重置**。曾试的
`credential.helperselector.selected=manager` **无效**，已弃用并清理。
修后 PortableGit 与 `D:\Git` 两个 git 裸 `git push` 均 2.4s 成功。

### 结果
- **两个提交已成功推送**，远端 `main = a5c0de2`（347ec45 + a5c0de2），本地与远端一致
- 含 token 的 trace 日志已扫描（0 处凭据）并删除，`.workbuddy/tmp/` 已清空

### 教训
- `git config --unset X` 不带 `--global` 时作用于**本地仓库**配置，非仓库目录会报
  `fatal: not in a git directory` 并静默失败 —— 改全局配置必须带 `--global`
- 「只读通道 OK、写入通道失败」不等于 TLS 问题，先看 HTTP 状态码与凭据环节

---

## 【补充】残留问题的量化排查：已认证 receive-pack 间歇无响应（当日 18:20）

### 现象
凭据修好后 push 能成功，但**约 40–50% 会死等**：请求已完全发出
（`Request completely sent off`），响应永不返回，一直挂到客户端超时。

### 排除法实验（全部**无效**，勿重复尝试）
| 变量 | 样本 | 结果 |
|---|---|---|
| 基线（schannel + H1） | 4 | 2/4 |
| `http.sslBackend=openssl` | 4 后 6 | 4/4 → 4/6（**4/4 是运气**） |
| `http.extraHeader=Connection: close` | 4 | 2/4 |
| `http.version=HTTP/2` | 4 | 2/4 |
| 预置 `Authorization` 跳过 401 | 5 | 3/5 |
- 已回退 `http.sslBackend` 试验配置，全局最终只保留 `credential.helper` 修复
- **重要教训**：n=4 的样本得出「openssl 4/4 = 修复」是**假阳性**，
  必须加大样本才能判定，否则会把运气当结论

### 定位边界
| 通道 | 结果 |
|---|---|
| `git ls-remote`（未认证 upload-pack） | ✅ 5/5，1.5–2.6s |
| 未认证 receive-pack（401） | ✅ 6/6，约 1.0s |
| **带认证 receive-pack（git）** | ❌ 约 50% 死等 |
| **带认证 receive-pack（系统 curl）** | ❌ **同样 code=000 死等** → 非 git 问题 |
| curl 复用连接连发 6 次 | ✅ 6/6 |
- 成功/失败**在时间上成簇**（一段全成、一段全败）→ 指向 mihomo 上游节点/链路抖动
- 观察期 `netstat` 见多个 `→127.0.0.1:7897` 卡在 `SYN_SENT`

### 结论与应对
- 根因分两层：① **凭据助手错位**（原始故障主因，已修）；
  ② **已认证 push 端点的间歇性无响应**（代理侧链路抖动，本地无解）
- 应对：`for i in 1 2 3 4 5; do git push origin main && break; sleep 2; done`
- 本次成果：**3 个提交全部推送成功**，远端 `main = 47fdb7a = 本地`，工作区干净
- 临时日志已扫描（0 处明文 token）并删除 `.workbuddy/tmp/`
